import { ethers } from 'ethers';

/**
 * 
 * @returns the current tick for a given Uniswap pool
 */
export async function getCurrentTick(/* Add what you need */): Promise<number> {
  return 205000;
}
